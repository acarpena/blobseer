/**
 * Generic empty list implementation (ends the recursively defined list).
 * Used to memorize a XML structure.
 * 
 * @author Cristina Basescu
 * @date 24.03.2010
 */

package interf.list;

public interface EIList<E> extends IList<E>{
}
